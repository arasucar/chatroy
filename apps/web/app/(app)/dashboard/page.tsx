export default function DashboardPage() {
  return (
    <main style={{ padding: "2rem" }}>
      <h1>Dashboard</h1>
      <p style={{ color: "var(--muted)", marginTop: "0.5rem" }}>
        Chat interface ships in Phase 3.
      </p>
    </main>
  );
}
